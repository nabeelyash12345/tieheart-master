import React from 'react';
import EmojiBoard from 'react-native-emoji-board';

const CustomEmojiBoard = (props) => {
    return (
        <EmojiBoard {...props} />                    
    )
}

export default CustomEmojiBoard;